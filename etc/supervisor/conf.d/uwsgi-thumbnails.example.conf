[program:uwsgi_thumbnails]
command=/usr/local/sbin/uwsgi
    --module generate
    --socket /tmp/uwsgi_thumbnails.sock
    --pythonpath /var/www/uwsgi_thumbnails
    --processes 5
    --master
    --harakiri 120
    --max-requests 5000
    --reaper
directory=/var/www
user=www-data
autostart=true
autorestart=true
stdout_logfile=/var/log/uwsgi_thumbnails.log
redirect_stderr=true
stopsignal=QUIT
